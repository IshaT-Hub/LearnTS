function addTwo(num: number):number{
    //num.toUpperCase();
    return num+2;
}

function signUpUser(name: string, email:string, idPaid: boolean = false){}

let LoginUser = (name: string, email:string, idPaid: boolean = false) =>{}

addTwo(5);
signUpUser("isha","i@i.com");
LoginUser("ish", "ish@g.com")
export{}

